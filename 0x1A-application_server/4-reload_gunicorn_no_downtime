#!/usr/bin/env bash
# Gracefully reloads Gunicorn using pgrep

pids=$(pgrep gunicorn)
if [ -n "$pids" ]; then
    echo "Reloading Gunicorn processes: $pids"
    kill -HUP "$pids"
else
    echo "No Gunicorn processes found."
fi
